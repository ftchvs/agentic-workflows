# Fictional repo triage example

Scenario: an unfamiliar sample repo has failing tests.

Use:

1. `repo-triage.md` to map structure and gates.
2. `multi-agent-review-loop.md` for discovery and risk review if the bug is
   ambiguous.
3. `learning-extractor.md` after the fix if the sequence was non-obvious.
