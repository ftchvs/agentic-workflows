# Private by default

The safest public workflow is generic by design.

Use synthetic examples. Replace private projects with fictional names. Remove
real paths, account IDs, customer names, internal terminology, calendars,
messages, and secrets.

A public workflow should teach the pattern without exposing the life or systems
that produced it.
